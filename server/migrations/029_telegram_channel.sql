-- ============================================================
--  باتِ تلگرامِ پمپ — کانال هم مقصدِ هشدار است
--
--  خواستهٔ صاحب سامانه (۱۴۰۵/۰۷/۱۳): «نمی‌دانم چطور ربات را به کانالِ مورد
--  نظرِ خودم وصل کنم.» کانال با گروه فرق دارد: بات باید «مدیرِ» کانال شود
--  و تلگرام هیچ `/start`ی در کانال نمی‌فرستد؛ پس وصل شدن از روی **کسی که
--  بات را مدیر کرد** است (همان حسابی که در گفت‌وگوی خصوصی وصل است).
-- ============================================================
ALTER TABLE telegram_chats DROP CONSTRAINT IF EXISTS telegram_chats_kind_check;
ALTER TABLE telegram_chats ADD CONSTRAINT telegram_chats_kind_check
  CHECK (kind IN ('private','group','channel'));
