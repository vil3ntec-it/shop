<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

  <!--
       به‌روزرسانی از داخلِ خودِ برنامه.

       این اجازه یعنی «این برنامه می‌تواند فایلِ نصبی را به نصب‌کنندهٔ
       اندروید بدهد». هزینه‌اش را بدانید: گوشی بارِ اول می‌پرسد که از این
       منبع اجازهٔ نصب هست یا نه، و همین اجازه است که سنجشگرها به آن
       حساس‌اند. در عوض، به‌روزرسانی داخلِ خودِ برنامه انجام می‌شود و
       کاربر سراغِ مرورگر نمی‌رود.

       فایل در پوشهٔ خودِ برنامه می‌نشیند و با FileProvider — نه با مسیرِ
       خام — به نصب‌کننده داده می‌شود.
  -->
  <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

  <uses-permission android:name="android.permission.CAMERA" />

  <!--
    یادآوریِ روزانه: موجودیِ تمام‌شده و قرضِ سررسیده. تا امروز هشدارها
    فقط داخلِ برنامه بودند و تا کسی بازش نمی‌کرد خبردار نمی‌شد.
  -->
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <uses-feature android:name="android.hardware.camera" android:required="false" />

  <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
  <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
  <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
  <uses-feature android:name="android.hardware.bluetooth" android:required="false" />

  <application
      android:allowBackup="true"
      android:dataExtractionRules="@xml/backup_rules"
      android:fullBackupContent="@xml/backup_rules_legacy"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:supportsRtl="true"
      android:usesCleartextTraffic="true"
      android:theme="@style/Theme.Tohid">

    <activity
        android:name=".MainActivity"
        android:exported="true"
        android:launchMode="singleTask"
        android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode|density|fontScale"
        android:windowSoftInputMode="adjustResize">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <provider
        android:name="androidx.core.content.FileProvider"
        android:authorities="${applicationId}.files"
        android:exported="false"
        android:grantUriPermissions="true">
      <meta-data
          android:name="android.support.FILE_PROVIDER_PATHS"
          android:resource="@xml/file_paths" />
    </provider>
  </application>
</manifest>
