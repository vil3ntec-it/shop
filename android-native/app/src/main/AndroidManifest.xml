<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

  <!--
       اجازهٔ «نصبِ برنامهٔ دیگر» (REQUEST_INSTALL_PACKAGES) عمداً اینجا
       نیست.

       این تنها اجازه‌ای در فهرستِ ما بود که معنی‌اش برای سیستم این است:
       «این برنامه می‌تواند برنامهٔ دیگری را روی گوشی بنشاند». همان چیزی
       که باعث می‌شد گوشی سرِ هر نصب هشدار بدهد و بپرسد از این منبع
       اجازه هست یا نه — و همان الگویی است که سنجشگرهای گوگل به آن
       حساس‌اند.

       به‌روزرسانی از بین نرفت: نسخهٔ تازه پیدا و نشان داده می‌شود، و
       گرفتنش به مرورگر سپرده می‌شود. مرورگر خودش اجازهٔ نصب دارد، پس
       کار همان‌قدر انجام می‌شود بدونِ اینکه برنامهٔ دکان چنین توانی
       داشته باشد.
  -->

  <uses-permission android:name="android.permission.CAMERA" />
  <uses-feature android:name="android.hardware.camera" android:required="false" />

  <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
  <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
  <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
  <uses-feature android:name="android.hardware.bluetooth" android:required="false" />

  <application
      android:allowBackup="true"
      android:dataExtractionRules="@xml/backup_rules"
      android:fullBackupContent="@xml/backup_rules_legacy"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:supportsRtl="true"
      android:usesCleartextTraffic="true"
      android:theme="@style/Theme.Tohid">

    <activity
        android:name=".MainActivity"
        android:exported="true"
        android:launchMode="singleTask"
        android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode|density|fontScale"
        android:windowSoftInputMode="adjustResize">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
  </application>
</manifest>
