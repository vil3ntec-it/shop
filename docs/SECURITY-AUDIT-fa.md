# 🛡️ ممیزیِ امنیتیِ سرورِ حساب — ۱۴۰۵/۰۷/۱۲ (نسخهٔ ۲.۹.۱)

این سند جوابِ پرامپتِ «سرورِ خانگی / Backend را مثلِ یک Production Backend
بررسی و Hardening کن» است، برای **سرورِ حساب** (`server/`، همین مخزن) — تنها
منبعِ حقیقتِ حساب، اشتراک، پلن، دستگاه و مجوز. سرورِ خانگیِ صاحب سامانه
(ریپوی `server`: پنل، تونل، درگاه، پشتیبانِ خارج از سایت) سندِ جدای خودش را
دارد: `docs/SECURITY-AUDIT-fa.md` در همان ریپو. برنامهٔ پمپ هم:
`native/docs/SECURITY-AUDIT-fa.md` در ریپوی پمپ.

⛔ قاعدهٔ این سند: **هیچ بندی بی سنجه «بسته شد» نوشته نمی‌شود.** هر چه
بسته شد نامِ آزمونش کنارش است؛ هر چه نشد، دلیل و راهِ درستش.

---

## A) معماری — آن‌چه واقعاً هست

```
برنامه‌ها (پمپ · دکان · اپِ کارمندان · وب)
        │ HTTPS
        ▼
Cloudflare Tunnel ─▶ پنلِ خانگی :4701 (درگاه account-proxy.js) ─▶ این سرور :3000 (فقط 127.0.0.1)
                                                                 │
                                                  PostgreSQL یا PGlite (همان کامپیوتر)
```

- **توکن‌ها مات‌اند، نه JWT**: ۳۲ بایتِ تصادفی، فقط SHA-256شان در `tokens`.
  دسترسی ۶۰ دقیقه، تازه‌سازی ۹۰ روز (چرخشی)، مدیر ۱۲ ساعت. هیچ کلیدِ
  امضای JWTی در کار نیست که لو برود.
- **مجوزِ آفلاین**: ES256 روی P-256، امضای خامِ P1363، ده‌روزه. کلیدِ خصوصی
  فقط روی این سرور است (`app_config.license_private_key` یا
  `LICENSE_PRIVATE_KEY` در محیط).
- **دو بخش، یک هویت**: `users` مشترک است؛ هر چیزِ دیگر (توکن، اشتراک،
  کد، پلن، دستگاه) با `app` یا جدولِ جدا از هم جداست.

---

## B) یافته‌ها — Critical / High / Medium / Low

| # | شدت | یافته | حال | سنجه |
|---|---|---|---|---|
| S1 | **Critical** | `GET /api/admin/plans` و `PATCH /api/admin/config` **کلِ** `app_config` را برمی‌گرداندند — یعنی **کلیدِ خصوصیِ امضای مجوز**، کلیدِ خصوصیِ پوش (VAPID)، رمزِ SMTP، کلیدِ پیامک. پنلِ خانگی همین پاسخ را بی فیلتر به مرورگرِ هر کاربرِ واردشده می‌داد. | ✅ بسته — `plans.publicConfig()` فهرستِ **سفید** است | `security-hardening` بندِ ۱ |
| S2 | **High** | `/pump/device/activate` (بی‌توکن) پمپ را از روی `deviceUid` پیدا می‌کرد و `register` توکنِ دستگاهِ صاحب را **جایگزین** می‌کرد. `deviceUid` راز نیست. | ✅ بسته — تمدیدِ «همان پمپ» فقط با توکنِ همان دستگاه | بندِ «⛔ شناسهٔ کامپیوترِ کسِ دیگری…» در `pump-device` |
| S3 | **High** | `max_devices` هیچ‌جا خوانده نمی‌شد؛ یک اشتراک هر تعداد کامپیوتری را باز می‌کرد. `/api/pump/license` برای **هر** `deviceUid`ی امضا می‌کرد. | ✅ بسته — سقف زیرِ `pg_advisory_xact_lock`، مجوز فقط برای دستگاهِ ثبت‌شده | بندهای ۲، ۲ب؛ `pump` «⛔ برای کامپیوترِ ثبت‌نشده…» |
| S4 | **High** | هیچ راهی برای جدا کردنِ دستگاه نبود؛ و upsertِ `register` دستگاهِ `revoked` را بی‌صدا `active` می‌کرد. | ✅ بسته — `revoke`/`restore` برای صاحب و مدیر، `revoked` می‌چسبد | بندِ ۳ |
| S5 | **High** | `/pump/device/bind` برای **هر** عضو (کارمند هم) توکنِ دستگاهِ همیشگی می‌داد؛ با حذفِ عضو، دستگاهش زنده می‌ماند. | ✅ بسته — فقط صاحب/مدیر؛ `bound_by_user_id` و باطل شدن با رفتنِ عضو | بندِ ۴ |
| S6 | **High** | شمارندهٔ تلاشِ کدِ ایمیلی «بخوان، بعد عددِ مطلق بنویس» بود — درخواست‌های هم‌زمان از سقفِ پنج بار رد می‌شدند. | ✅ بسته — `UPDATE … attempts < max RETURNING` پیش از سنجیدن؛ مصرف با `consumed_at IS NULL` | بندِ ۶ |
| S7 | Medium | `grant` بی قفل: دو تمدیدِ هم‌زمان ⇒ دو کد خرج، یک دوره اضافه. | ✅ بسته — قفلِ هر مشتری در تراکنش | بندِ ۹ (روی PostgreSQLِ واقعی پیش از اصلاح سرخ بود) |
| S8 | Medium | اشتراکِ **معلق** با خرج کردنِ هر کدی زنده می‌شد. | ✅ بسته — `subscription_suspended`؛ فقط مسیرِ مدیر (`allowUnsuspend`) | بندِ ۵ |
| S9 | Medium | `setStatus` هر گذاری را می‌پذیرفت (لغو ⇒ فعال). | ✅ بسته — `TRANSITIONS` | بندِ ۵ب |
| S10 | Medium | بازپخشِ توکنِ تازه‌سازیِ چرخیده پس از ارفاق فقط ۴۰۱ بود — دزد با زنجیرهٔ زنده می‌ماند؛ دو تازه‌سازیِ هم‌زمان دو زنجیرهٔ زنده می‌ساختند. | ✅ بسته — `refresh_reused` کلِ خانواده را می‌بندد؛ `markRotated` شرطی | بندهای ۷، ۷ب |
| S11 | Medium | سقفِ مجوز `ends_at`ِ خام بود — در روزهای مهلت، سرور «فعال» می‌گفت و مجوزی می‌داد که همان لحظه منقضی بود. | ✅ بسته — `activeUntil = graceEndsAt`؛ مجوزِ پیش‌منقضی امضا نمی‌شود | بندِ ۸ |
| S12 | Medium | مجوز `kid` نداشت ⇒ چرخاندنِ کلید یعنی قفل شدنِ همهٔ نصب‌ها. | 🟡 نیمه — `kid` حالا هست؛ برنامهٔ پمپ مجموعه‌کلیدِ درون‌برنامه‌ای را می‌پذیرد (سندِ پمپ). **خودِ چرخاندن** هنوز انجام نشده (بخشِ «کلیدها») | بندِ ۸ |
| S13 | Medium | `X-Forwarded-For` چپ‌ترین بخش را می‌خواند. | ✅ بسته — راست‌ترین. (پشتِ درگاهِ خانگی از قبل امن بود چون درگاه بازنویسی می‌کند) | — |
| S14 | Medium | کدِ پیوستن: `FOR UPDATE` بیرونِ تراکنش (سقف رد می‌شد، دو «صاحب» هم‌زمان)؛ کامپیوتر می‌توانست کدِ «صاحب» بسازد. | ✅ بسته — یک تراکنش، `owner_user_id IS NULL`، سقفِ نقش `manager` | بندِ ۱۰ |
| S15 | Low | `DELETE /api/me/devices/:id` توکن‌ها را بی سنجشِ مالکیت باطل می‌کرد. | ✅ بسته | — |
| S16 | Low | `endsAt`ِ اشتراکِ پمپ از پنل سنجیده نمی‌شد. | ✅ بسته — `v.timestamp` | — |
| S17 | Medium | ورودِ مدیر MFA ندارد؛ قفلِ مدیر سراسری است (DoS). | ⏳ باز — پیشنهاد: TOTP (همان `lib/totp` پنلِ خانگی) | — |
| S18 | Medium | شمارش‌پذیریِ حساب از `register/start` (۴۰۹) و زمانِ `password/forgot`؛ سقفِ کد فقط به‌ازای مقصد است، نه IP. | ⏳ باز — نیاز به سطلِ IP کنارِ سطلِ مقصد | — |
| S19 | Medium | شماره تلفن بی تأیید روی حساب می‌نشیند (`PUT /api/me`). | ⏳ باز — OTP پیش از نشستنِ شماره | — |
| S20 | Low | scrypt با `N=16384`؛ پیشنهادِ امروز `2^17`. | ⏳ باز — با rehash پس از ورود | — |
| S21 | Low | دورهٔ آزمایشی به‌ازای هر پمپِ تازه است، نه هر آدم/دستگاه. | ⏳ باز — دفترِ `trial_used` | — |
| S22 | Info | لغو و بازگرداندنِ پول مجوزِ صادرشده را باطل نمی‌کند؛ حداکثر ده روز (عمرِ مجوز) زنده می‌ماند. | پذیرفته — عمرِ کوتاه همان راهِ باطل‌سازی است | — |

---

## C) API و دیتابیس — جدا

### جدولِ مسیرهای حساس

| مسیر | احراز | مجوز | سقفِ نرخ | Idempotency | اعتبارسنجی | دفترِ رخداد |
|---|---|---|---|---|---|---|
| `POST /api/auth/register/*` | — | — | مقصد + IP | بلیتِ یک‌بارمصرف | ایمیل، رمز، شرایط | ✅ |
| `POST /api/auth/login` | — | — | IP + قفلِ (شناسه+IP) و سراسری | — | ✅ | موفق |
| `POST /api/auth/refresh` | تازه‌سازی | همان بخشِ ردیف | ۱۲۰/IP | ارفاقِ ۳۰ث + **شرطی** | — | بازپخش ✅ |
| `POST /api/auth/:app/verify` | request_id+کد | — | لبه + **سقفِ اتمی** | مصرفِ شرطی | ۶ رقم | ✅ |
| `POST /api/pump/device/activate` | کد (± توکنِ دستگاه) | کدِ همان پمپ | join/IP | upsert | ✅ | ✅ |
| `POST /api/pump/device/bind` | حسابِ پمپ | **صاحب/مدیر** | join/IP | upsert، سقفِ قفل‌دار | ✅ | ✅ |
| `POST /api/pump/device/license` | توکنِ دستگاه (`active`) | همان پمپ | general | — | — | — |
| `POST /api/pump/license` | حسابِ پمپ | **دستگاهِ ثبت‌شده** | general | — | ✅ | — |
| `GET/POST /api/pump/devices…` | حسابِ پمپ | صاحب/مدیر؛ برگرداندن فقط صاحب | general | — | ✅ | ✅ |
| `POST /api/admin/pump/stations/:id/devices/:d/(revoke\|restore)` | مدیر | `requireAdmin` | general | — | ✅ | ✅ |
| `GET /api/admin/plans` | مدیر | `requireAdmin` | general | — | — | — |
| `POST /api/admin/pump/subscriptions` | مدیر | `requireAdmin` | general | ⏳ (دکمهٔ دوبار) | ✅ | ✅ |

⏳ `Idempotency-Key` روی دادنِ اشتراک از پنل هنوز نیست؛ دوبار-کلیک دو دوره
می‌دهد. پنلِ خانگی دکمه را تا پاسخ خاموش نگه می‌دارد — ولی سرور خودش باید
بپذیرد. کارِ مانده.

### دیتابیس

- ✅ **یک اشتراکِ زنده برای هر مشتری** با ایندکسِ یکتای جزئی
  (`idx_station_subs_one_live`)، کلید خارجی با `ON DELETE CASCADE` روی
  دستگاه‌ها، و `UNIQUE (station_id, device_uid)`.
- ✅ همهٔ پرس‌وجوها پارامتری‌اند؛ تنها چیزی که درون‌یابی می‌شود نامِ جدولِ
  ثابت (`tenancy`) است.
- ✅ عملیاتِ حساس **اتمی** شدند: `grant` (قفلِ مشتری)، `register`/`restore`
  (قفلِ پمپ)، `redeemJoinCode` (یک تراکنش)، `vip.redeem` (از قبل
  `FOR UPDATE`)، سنجشِ کد (UPDATEِ شرطی).
- ⚠️ `pg_advisory_xact_lock(hashtext(...))` در PGlite هم کار می‌کند (سنجیده
  شد) ولی آن‌جا کلِ دیتابیس تک‌اتصالی است؛ مسابقه‌ها فقط روی PostgreSQLِ
  واقعی دیده می‌شوند — پس سنجه‌های هم‌زمانی باید روی PostgreSQL هم بدوند
  (CI هر دو را می‌دواند).
- مهاجرتِ ۰۲۶: `station_devices.bound_by_user_id`.

---

## D) ماشینِ حالتِ اشتراک

```
             grant (مدیر / کد / دورهٔ آزمایشی)
                         │
   pending ──▶ active ──▶ suspended ──▶ active   (فقط مدیر: allowUnsuspend)
      │          │  │         │
      └──▶ cancelled ◀────────┘
                 │  └──▶ expired   (یا خودکار: now > ends_at + grace_days)
```

- `TRANSITIONS` در `lib/subscriptions.js` تنها نقشه است. `cancelled` و
  `expired` پایانی‌اند؛ برگشت فقط با `grant` (مدت + ردِ خودش).
- «فعال» همیشه با **ساعتِ سرور** حساب می‌شود (`stateOf`)، هیچ‌وقت از روی
  چیزی که کلاینت می‌گوید.
- مهلت (`grace_days`) مالِ **اشتراک** است و سرور تا پایانش مجوز می‌دهد؛
  ارفاقِ **آفلاینِ** برنامه چیزِ جداست (سندِ پمپ).
- تغییرِ پلن وسطِ دوره: `grant` روی اشتراکِ زنده پلن و فهرستِ قابلیت را
  **جایگزین** می‌کند و پایان را جلو می‌برد — مانده‌ی دورهٔ قبل با پلنِ تازه
  می‌گذرد. عمدی و ثبت‌شده در `*_history`.
- بازگرداندنِ پول (`DELETE /payments/:id`) اشتراک را دست نمی‌زند — مدیر باید
  صریح لغو کند. ⏳ پیشنهاد: پرسشِ «اشتراک هم لغو شود؟» در همان کادر.

## E) ماشینِ حالتِ دستگاه

```
 (نبوده) ──activate/bind──▶ active ──revoke (صاحب/مدیر)──▶ revoked
               ▲   │ نصبِ دوباره = همان ردیف، توکنِ تازه        │
               │   └─ رفتنِ عضوی که بندش کرده ─▶ revoked       │
               └───────── restore (فقط صاحب/مدیرِ سامانه، با سقف) ◀┘
```

- دستگاهِ **تازه** فقط اگر `count(active) < max_devices` (از اشتراکِ زنده،
  وگرنه ۱۰) — زیرِ قفلِ همان پمپ.
- `revoked` با activate/bind **زنده نمی‌شود** (`device_revoked`).
- پمپِ خاموش ⇒ همهٔ دستگاه‌هایش خاموش (`bySecret`).
- توکنِ دستگاه عمداً انقضا ندارد (کامپیوترِ پمپ ماه‌ها آفلاین می‌ماند)؛ جایش
  **مجوز** ده‌روزه است و **جدا کردن** همان لحظه اثر می‌کند.

## F) جریانِ مجوز — آنلاین و آفلاین

آنلاین (هر ۱۰ دقیقه یا با تغییرِ حالِ اشتراک):

1. برنامه `POST /api/pump/device/license` با توکنِ دستگاه.
2. سرور: دستگاه `active`؟ پمپ `active`؟ ⇒ `entitlementOf` با ساعتِ سرور.
3. `free` ⇒ `license: null` (برنامه مجوزِ کهنه را پاک می‌کند ⇒ ارفاقی نمی‌ماند).
4. وگرنه `issue`: `{iss, aud, duid, stn, sub, iat, nbf=iat−60s,
   exp=min(iat+10d, graceEndsAt), sub_ends, feat, core, plan}` با سرآیندِ
   `{alg:ES256, typ:TLIC, kid}`.

آفلاین (سمتِ برنامه — شرحِ کامل در سندِ پمپ): امضا ⇐ کلیدِ درون‌برنامه‌ای یا
قفل‌شده؛ `aud/duid/stn`؛ `exp` با ساعتی که فقط جلو می‌رود؛ ارفاقِ ۱۴روزه
**فقط** از روی همان مجوزِ امضاشده و **با همان فهرستِ قابلیت**.

ضدِ بازپخش: مجوز به `duid` و `stn` بسته است و `exp` کوتاه دارد؛ برنامه
کفِ ساعت را از `iat` مجوزهای دیده‌شده بالا می‌برد، پس مجوزِ کهنه‌تر با
عقب بردنِ ساعت زنده نمی‌شود.

---

## G) مدلِ تهدید

| تهدید | اثر | تشخیص | پیشگیری | بازیابی |
|---|---|---|---|---|
| توکنِ حسابِ دزدیده | ورود به حساب تا ۶۰ دقیقه | — | عمرِ کوتاه، بسته شدن با عوض کردنِ رمز | «خروج از همه» |
| توکنِ تازه‌سازیِ دزدیده | ماندگاری تا ۹۰ روز | `auth.refresh_reused` | چرخشی + بستنِ خانواده با بازپخش | خودکار؛ ورودِ دوباره |
| حسابِ دزدیده (رمز) | همه‌چیزِ آن حساب | `login_attempts` | scrypt، قفل (شناسه+IP)، سراسری | بازیابیِ رمز با کد (همهٔ نشست‌ها می‌افتند) |
| بازپخشِ مجوز | باز ماندنِ قفل‌ها | — | `exp` ده‌روزه، `duid`، `stn`، کفِ ساعت در برنامه | سرور مجوزِ تازه نمی‌دهد |
| مجوزِ جعلی | باز شدنِ همهٔ قفل‌ها | — | ES256؛ کلیدِ خصوصی فقط روی سرور؛ **کلیدِ درون‌برنامه‌ای** | — |
| **کلیدِ امضای لو رفته** | جعلِ مجوز برای همه | — | S1 بسته شد؛ کلید را از `app_config` به `LICENSE_PRIVATE_KEY` ببرید | چرخاندنِ کلید (بخشِ «کلیدها») |
| پاسخِ ساختگیِ API (MITM) | باز شدنِ قفل در برنامه | — | TLS؛ برنامه **پاسخِ امضانشده** را برای ارفاق نمی‌پذیرد | — |
| برنامهٔ دست‌کاری‌شده | دور زدنِ قفلِ **محلی** | — | قفلِ واقعیِ کارهای ابری روی سرور است (پوشهٔ ابری `cloud` را هر بار می‌سنجد) | — |
| درخواست‌های فعال‌سازیِ مخرب | پمپِ یتیم، حدسِ کد | — | join/IP؛ کدِ خرج‌شده پمپ نمی‌سازد | — |
| مسابقه (Race) | دو برابر روز، سقفِ رد شده | — | قفل‌ها و UPDATEهای شرطیِ بخشِ C | — |
| دیتابیسِ لو رفته | ایمیل، هشِ رمز، **کلیدِ خصوصی** | — | توکن‌ها و کدها هش‌شده‌اند؛ کلید را از DB بیرون ببرید | چرخاندنِ کلید + `API_SECRET`/`OTP_SECRET` |
| مدیرِ دزدیده | همه‌چیز | `audit_log` | ⏳ MFA؛ نمایشِ کد فقط `superadmin` و با ثبت | عوض کردنِ رمز؛ `admin-bootstrap` |
| کامپیوترِ پمپِ دزدیده | پوشه و چتِ آن پمپ | `last_seen_at` | **جدا کردنِ دستگاه** (S4) | — |

---

## H) چک‌لیستِ Hardening

- [x] رازها از هیچ پاسخِ API بیرون نمی‌روند (`publicConfig`)
- [x] سقفِ دستگاه، جدا کردن، و سقفِ نقش برای بند شدن
- [x] سنجشِ اتمیِ کد و مصرفِ یک‌بار
- [x] چرخشِ تازه‌سازی با تشخیصِ بازپخش
- [x] ماشینِ حالتِ اشتراک و قفلِ تمدید
- [x] `kid` در مجوز
- [x] `TRUST_PROXY` با راست‌ترین بخش
- [ ] `LICENSE_PRIVATE_KEY` در محیط، نه در `app_config` (پیش‌نیاز: چرخاندن)
- [ ] `BACKUP_PASSPHRASE` همیشه پر (امروز اختیاری است)
- [ ] TOTP برای مدیر
- [ ] سطلِ IP روی `/otp/*` · `/register/*` · `/password/*`
- [ ] `Idempotency-Key` روی دادنِ اشتراک
- [ ] OTP پیش از نشستنِ شماره تلفن

---

## I) کلیدها — کجا، چه کسی، چرخاندن، لو رفتن

| کلید | کجا | دسترسی | چرخاندن | اگر لو رفت |
|---|---|---|---|---|
| امضای مجوز (ES256) | `app_config.license_private_key` (یا `LICENSE_PRIVATE_KEY`) | فقط پروسهٔ سرور | ۱) کلیدِ تازه بسازید، `kid`ش را در `LICENSE_PUBLIC_KEYS`ِ ساختِ برنامه بگذارید و نسخهٔ تازه را منتشر کنید؛ ۲) پس از به‌روز شدنِ نصب‌ها، `LICENSE_PRIVATE_KEY` را به کلیدِ تازه عوض کنید | همان دو گام، فوری؛ مجوزهای جعلی با `exp` ده‌روزه‌شان تمام می‌شوند |
| `API_SECRET` | محیط / `secrets.json`ِ پنل | پروسهٔ سرور | عوض کردن ⇒ همهٔ نشست‌ها و کدهای پیوستن باطل | همان |
| `OTP_SECRET` | محیط | پروسهٔ سرور | عوض کردن ⇒ کدهای زنده باطل (۵ دقیقه) | همان |
| TLS | Cloudflare (لبهٔ تونل) | Cloudflare | خودکار | — |
| رمزِ دیتابیس | محیط (`DATABASE_URL`)؛ PGlite رمز ندارد | کاربرِ سیستم | عوض کردنِ رمزِ نقش در PostgreSQL | — |
| `BACKUP_PASSPHRASE` | محیط | صاحبِ سامانه | فایل‌های قدیم با عبارتِ قدیم باز می‌شوند — نگهش دارید | پشتیبان‌های قبلی را رمزِ تازه کنید |
| VAPID (پوش) | `app_config.vapid_private` | پروسهٔ سرور | عوض کردن ⇒ مرورگرها باید دوباره عضو شوند | همان |
| SMTP / پیامک | `app_config` | پروسهٔ سرور | در سرویس‌دهنده | همان |

⛔ **کلیدِ خصوصی را هیچ‌وقت برای «راحتی» از `app_config` پاک نکنید تا وقتی
`LICENSE_PRIVATE_KEY` همان را دارد** — کلیدِ تازه یعنی `key_mismatch` روی
همهٔ نصب‌های امروز.

---

## J) همهٔ شرایطِ مسابقه‌ای که پیدا شد

| جا | پیش از این | حالا |
|---|---|---|
| `subs.grant` | خواندن بی قفل ⇒ یک دوره گم | قفلِ مشتری |
| `devices.register` | بشمار-بعد-بنویس ⇒ سقف رد | قفلِ پمپ |
| `devices.restore` | — | قفلِ پمپ + سقف |
| `redeemJoinCode` | `FOR UPDATE` بی تراکنش ⇒ سقف رد، دو صاحب | یک تراکنش + `owner_user_id IS NULL` |
| `login-codes.verifyCode` | شمارندهٔ مطلق ⇒ صدها حدس | UPDATEِ شرطی |
| `otp.verify` | همان شکل (نیمه‌امن با سطلِ حافظه) | UPDATEِ شرطی |
| `/auth/refresh` | دو زنجیرهٔ زنده | `markRotated` شرطی + تلاشِ دوباره |
| `vip.redeem` | ✅ از قبل `FOR UPDATE` در تراکنش | — |
| `discounts.quote → useCode` | ⏳ غیرِاتمی (فقط مسیرِ مدیر) | باز |

---

## M) آن‌چه برای سرورِ خانگی واقعاً شدنی است

- یک پروسه، یک کامپیوتر: قفلِ حافظه و `advisory lock` بس است؛ Redis و صف
  لازم نیست.
- پشتیبان: `pg_dump`/`dumpDataDir` روزانه + نسخهٔ رمزشدهٔ خارج از سایت از
  راهِ پنلِ خانگی (سندِ همان ریپو).
- چرخاندنِ کلید: هیچ HSMی لازم نیست؛ دو گامِ بخشِ I با یک نسخهٔ برنامه.

آزمون: `test/security-hardening.test.js` (۱۳ بند — روی PostgreSQLِ واقعی
پیش از اصلاح ۱۲ تا سرخ، پس از آن همه سبز).
